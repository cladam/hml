@server(port: 8080, public: true) {
    host: "localhost"
    name: "api"
    timeout: 30s
}

@database(driver: "postgres") {
    host: "db.internal"
    port: 5432
    pool_size: 10
    ssl: true
}

@cache(driver: "redis") {
    host: "cache.internal"
    ttl: 300s
}
